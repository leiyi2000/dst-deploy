[project]
name = "dst-deploy"
version = "0.1.0"
description = "dst deploy"
authors = [
    {name = "ylei", email = ""},
]
dependencies = []
requires-python = ">=3.12"
readme = "README.md"
license = {text = "MIT"}

[build-system]
requires = ["pdm-backend"]
build-backend = "pdm.backend"
